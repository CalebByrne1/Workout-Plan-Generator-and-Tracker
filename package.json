{
  "name": "iron-ledger",
  "version": "1.1.0",
  "private": true,
  "description": "A four-day upper/lower workout tracker that runs entirely in the browser.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "node build.mjs && node test/logic.mjs && node test/sync.mjs && node test/browser.mjs",
    "test:sync": "node test/sync.mjs",
    "test:logic": "node test/logic.mjs",
    "test:browser": "node test/browser.mjs"
  }
}
